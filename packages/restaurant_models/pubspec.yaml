name: restaurant_models
description: Shared domain models for the restaurant application.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: '>=3.22.0'

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^5.0.2
  collection: ^1.18.0
  intl: ^0.20.2

dev_dependencies:
  lints: ^3.0.0
